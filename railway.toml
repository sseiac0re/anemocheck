[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --worker-class gevent -w 1 --worker-connections 1000 --timeout 120 --bind 0.0.0.0:$PORT app:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
